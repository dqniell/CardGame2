const card1=document.getElementsById("card1");
var matches=0;
var attempts=0;
var currCardsFlipped=0;

card1.addEventListener("click", flipcard())


card.addEventListener("click", flipCard());

function flipCard(){
    
}
